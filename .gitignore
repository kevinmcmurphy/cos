# User config (contains PII — database IDs, email domains, company names)
config.md
me.md
*.local.md

# Local daily-sweep reports (contain PII — email subjects, senders, body snippets)
reports/daily-sweeps/*
!reports/daily-sweeps/.gitkeep

# OS files
.DS_Store
BUILD.md
